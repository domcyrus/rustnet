# 観測された VLAN ID

> **未リリース:** `main` の開発版向けです。v1.6.0 には含まれません。

Details の Connection セクションにある **Observed VLANs** は、その接続で捕捉した 802.1Q ID を重複なしの昇順で表示します。クリックするとコピーできます。プレースホルダーはタグを観測していないことを示し、タグのない通信であることを保証しません。VID `0` は優先度タグであり、VLAN への所属を示すものではありません。後続のパケットでタグが見えなくても、以前の観測結果は保持されます。

ヘッドレス JSON/JSONL の接続レコードには `observed_vlan_ids` 配列が含まれ、未観測の場合は空配列になります。これは schema バージョン 1 への追加フィールドです。JSON イベントログと PCAP sidecar では、配列が空でない場合に同じフィールドを出力します。内容は各レコードの生成時点で判明していた ID です。

対象は、捕捉した Ethernet、Linux SLL、SLL2 フレーム内に残っているタグです。OS や NIC によるタグの除去や macOS PKTAP の処理によって、タグが見えない場合があります。既存の単一 802.1Q タグの解析を使用し、スタックされた VLAN タグの解析は追加しません。

接続は引き続きプロトコルとエンドポイントの組で集約されます。複数の ID はその行での観測結果であり、VLAN タグのスタックや VLAN ごとの接続分離を意味しません。

[使用ガイド（英語）](USAGE.md#observed-vlan-ids) · [使用指南（简体中文）](USAGE.zh-CN.md#已观察到的-vlan-id)
